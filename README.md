# Initial PC Setup (Мастер первоначальной настройки)

**Версия 2.2.0 (с предварительным просмотром и подтверждением)**

## О проекте

PowerShell-скрипт с графическим интерфейсом WPF, предназначенный для быстрой первоначальной настройки нового компьютера в корпоративной среде.  
Скрипт автоматизирует рутинные операции:

- Резервное копирование текущих сетевых настроек
- Установка статического IP-адреса, маски, шлюза и DNS-серверов
- Переименование компьютера
- Ввод в домен Active Directory с возможностью указания OU
- Настройка системного прокси (опционально)
- Ведение лога всех действий

Процесс настройки организован в виде **двухшагового мастера** с кнопками «Далее» и «Назад», что делает интерфейс интуитивно понятным и привычным для пользователей.

---

## Новые возможности версии 2.2.0

- ✅ **Предварительный просмотр и подтверждение** – перед применением настроек отображается окно со сводкой всех параметров (старые и новые значения). Пользователь может проверить данные и подтвердить или отменить настройку. Это снижает риск случайных ошибок.
- ✅ **Двухшаговый мастер** – ввод разделён на два логических этапа:
  - Шаг 1: выбор адаптера и настройка IP-параметров.
  - Шаг 2: имя компьютера, домен, OU, прокси и прогресс-бар.
- ✅ **Пошаговая валидация** – поля проверяются сразу на соответствующем шаге, сообщения об ошибках выводятся до перехода к следующему этапу.
- ✅ **Автоматический запрос прав администратора (UAC)** – при запуске скрипт сам определяет, есть ли права администратора, и если их нет, перезапускается с повышенными правами через UAC-запрос.
- ✅ **Валидация всех вводимых полей** – IP, маска (префикс), имя компьютера, DNS, порт прокси – проверяются перед применением настроек.
- ✅ **Выбор сетевого адаптера** – можно выбрать любой активный физический адаптер из выпадающего списка (кнопка «Обновить» для пересканирования).
- ✅ **Указание OU (подразделения)** – при вводе в домен можно указать конкретное подразделение (или оставить пустым для контейнера по умолчанию).
- ✅ **Настройка прокси-сервера** – чекбокс «Настроить системный прокси» позволяет задать адрес и порт для прокси (применяется и для WinHTTP).
- ✅ **Проверка доступности контроллера домена** – перед вводом в домен выполняется DNS-запрос и ping; при недоступности выводится предупреждение.
- ✅ **Прогресс-бар** – отображает ход выполнения операций (настройка IP, прокси, переименование, ввод в домен).
- ✅ **Полная совместимость с PowerShell 5.1** – скрипт работает на всех поддерживаемых версиях Windows без необходимости установки дополнительных модулей.
- ✅ **Улучшенное логирование** – в лог включается информация о выбранном адаптере, OU и настройках прокси.

---

## Текущие возможности (полный список)

- Автоматический запрос прав администратора (UAC) при запуске
- Двухшаговый мастер с пошаговой валидацией
- Окно предварительного просмотра и подтверждения настроек
- Создание бэкапа текущих параметров IP на рабочем столе
- Настройка статического IPv4 и DNS (с выбором адаптера)
- Смена имени компьютера с проверкой корректности
- Присоединение к домену Active Directory с указанием OU
- Настройка системного прокси (опционально)
- Проверка доступности контроллера домена перед вводом
- Поддержка нескольких сетевых адаптеров
- Валидация всех вводимых данных
- Логирование всех операций
- Графический интерфейс (WPF) с возможностью редактирования в Visual Studio
- Прогресс-бар в реальном времени

---

## Системные требования

- Windows 10/11 (или Windows Server с GUI)
- PowerShell 5.1 (встроен в Windows) или новее
- Учётные данные с правами на добавление ПК в домен
- Доступность контроллера домена по сети (DNS должен быть настроен)

> **Примечание:** права администратора запрашиваются автоматически при запуске, поэтому не нужно запускать PowerShell от имени администратора вручную.

---

## Инструкция по использованию

1. Скачайте файл `InitialSetup.ps1`.
2. Запустите скрипт любым способом:
   - **Двойным кликом** по файлу (если .ps1 ассоциирован с PowerShell).
   - Через PowerShell: `.\InitialSetup.ps1`.
3. При первом запуске появится запрос UAC на повышение прав – нажмите **Да**.
4. Откроется окно мастера:
   - **Шаг 1 (Сетевые параметры):**
     - Выберите сетевой адаптер из списка (при необходимости нажмите «Обновить»).
     - Введите новый IP-адрес, маску (префикс), шлюз и предпочтительный DNS-сервер.
     - Дополнительный DNS-сервер можно указать или оставить пустым.
     - Нажмите **«Далее»** – поля будут проверены; при ошибках вы увидите сообщение.
   - **Шаг 2 (Компьютер и домен):**
     - Введите новое имя компьютера, домен и (опционально) подразделение OU.
     - При необходимости включите настройку прокси и укажите адрес с портом.
     - Нажмите **«Готово»** – все поля будут проверены, а затем выполняется проверка доступности контроллера домена.
5. После успешной проверки откроется **окно предварительного просмотра** со сводкой всех параметров (текущие и новые значения). Проверьте данные и нажмите:
   - **«Применить»** – для начала настройки.
   - **«Отмена»** – чтобы вернуться в мастер и исправить данные.
6. При появлении окна введите учётные данные администратора домена (DOMAIN\Admin или admin@domain.local).
7. Дождитесь завершения операций – прогресс-бар покажет этапы.
8. После завершения будет предложено перезагрузить компьютер (рекомендуется).

---

## Планы по развитию (TODO) – будущие улучшения

- [ ] Создание исполняемого .exe файла (уже можно скомпилировать через PS2EXE)
- [ ] Автоматическое получение списка OU из Active Directory для выбора из выпадающего списка
- [ ] Поддержка настройки нескольких DNS-серверов (более двух)
- [ ] Возможность отката изменений при ошибке (восстановление из бэкапа)
- [ ] Сохранение настроек в JSON/XML для повторного использования
- [ ] Автоматическое определение свободного IP в подсети (возврат функции, если потребуется)

---

## Обратная связь

Проект активно развивается. Ваши предложения, замечания и вопросы приветствуются!  
Свяжитесь с автором или создайте **Issue** в этом репозитории.

---

*Версия 2.2.0 – полностью рабочая, с удобным двухшаговым мастером, пошаговой валидацией и окном подтверждения.*